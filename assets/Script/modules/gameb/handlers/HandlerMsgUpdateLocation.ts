import { RoomInterface } from "../RoomInterface";

/**
 * 响应服务器位置更新通知
 */
export namespace HandlerMsgUpdateLocation {
    export const onMsg = async (msgData: ByteBuffer, room: RoomInterface): Promise<void> => {
        // TODO:
    };
}
